# Reporting

Functions to summarize QC outcomes across multiple deposits and produce curator-facing reports.

## Function: `qc_summary_report()`

**Purpose:** Aggregate validation results and export HTML/CSV summaries.

```{r}
# qc_summary_report("outputs/validation-logs", out = "reports/qc.html")
```
