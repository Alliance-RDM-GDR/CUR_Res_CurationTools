{
  "hash": "487289fd19c765d9e233526432ca1647",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"XML Files\"\nauthor: \"Daniel Manrique-Castano\"\ndate: \"2025-12-05\"\nformat: \n  html:\n    toc: true\n    toc-location: left\n    code-fold: true\nparams:\n  target_dir: \"data/Inspect_xml/\"\n---\n\n\n\n\n\n\n\n\n## Overview\n\nThis notebook inspects **Extensible Markup Language (XML)** files (`.xml`). XML is a markup language that defines a set of rules for encoding documents in a format that is both human-readable and machine-readable.\n\n**Key Curation Objectives:**\n\n1.  **Inventory:** Verify the file structure and read availability.\n2.  **Structure Analysis:** Identify the root element and the general hierarchy.\n3.  **Metadata Extraction:** Extract attributes and key node names to understand the schema and content.\n\n### How to use this notebook\n\n-   **Purpose:** Produce a structural summary of XML files to support schema verification and downstream transformation planning.\n-   **Inputs:** A directory containing `.xml` files. No prior knowledge of the schema is required.\n-   **Process overview:** (1) select the directory, (2) list XML files, (3) parse each file to capture root nodes, key tags, and attributes, (4) handle parsing errors without stopping the workflow, and (5) write a CSV report to `Results/Inspect_xml/`.\n-   **Outputs:** A dated CSV that records file validity, primary elements, and notable attributes for each XML file.\n-   **Interpretation guidance:** Files that fail to parse or lack expected root elements should be returned to depositors or converted to a stable schema (e.g., via XSLT) before ingest.\n\n------------------------------------------------------------------------\n\n## Setup\n\nWe use the `xml2` package, which is a wrapper around the comprehensive `libxml2` C library.\n\n### R Packages\n\nIf you do not have the required packages, run this command once in your R console:\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# install.packages(c(\"tidyverse\", \"xml2\", \"rstudioapi\"))\n```\n:::\n\n\n\n\n\n\n\n\n### Load libraries\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(tidyverse)\nlibrary(xml2)\nlibrary(rstudioapi)\n```\n:::\n\n\n\n\n\n\n\n\n## Select Target Directory\n\nSelect the folder containing the XML files.\n\nNote: If running interactively, a dialog box will appear. Otherwise, it defaults to the target_dir parameter.\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 1. Try to select interactively if in RStudio\nif (interactive() && .Platform$OS.type == \"windows\") { \n  selected_dir <- rstudioapi::selectDirectory(caption = \"Select XML Directory\")\n} else {\n  selected_dir <- NULL\n}\n\n# 2. Logic to determine final directory\nif (!is.null(selected_dir)) {\n  target_dir <- selected_dir\n} else {\n  target_dir <- params$target_dir\n}\n\nprint(paste(\"Analyzing directory:\", target_dir))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"Analyzing directory: data/Inspect_xml/\"\n```\n\n\n:::\n:::\n\n\n\n\n\n\n\n\n## Find XML Files\n\nWe scan the directory for files ending in .xml.\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nxml_files <- list.files(\n  path = target_dir,\n  pattern = \"\\\\.xml$\", \n  recursive = TRUE, \n  full.names = TRUE, \n  ignore.case = TRUE\n)\n\nprint(paste(\"Found\", length(xml_files), \"XML files.\"))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"Found 2 XML files.\"\n```\n\n\n:::\n\n```{.r .cell-code}\nhead(xml_files)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"data/Inspect_xml/LaurentEtAl2025_BG_grid_H3.nc.aux.xml\"                                \n[2] \"data/Inspect_xml/LaurentEtAl2025_BG_H2_addTA_HerringCove_particulate_airsea.nc.aux.xml\"\n```\n\n\n:::\n:::\n\n\n\n\n\n\n\n\n## Generate Data Dictionary\n\nThis code iterates through the files and extracts their internal structure.\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nmessage(\"Generating Data Dictionary...\")\n\nreport <- purrr::map_dfr(xml_files, function(file_path) {\n  \n  fname <- basename(file_path)\n  \n  tryCatch({\n    # Read the XML file\n    doc <- read_xml(file_path)\n    \n    # Get Root Node\n    root <- xml_root(doc)\n    root_name <- xml_name(root)\n    \n    # Get Attributes of Root\n    attrs <- xml_attrs(root)\n    attrs_str <- if (length(attrs) > 0) paste(names(attrs), attrs, sep=\"=\", collapse=\"; \") else \"\"\n    \n    # Get Children Counts\n    children <- xml_children(root)\n    num_children <- length(children)\n    \n    # Get unique child names (to see structure)\n    child_names <- unique(xml_name(children))\n    child_names_str <- paste(child_names, collapse = \", \")\n    \n    tibble(\n      FileName = fname,\n      RootNode = root_name,\n      NumChildren = num_children,\n      ChildNodes = substr(child_names_str, 1, 100), # Truncate\n      RootAttributes = substr(attrs_str, 1, 100), # Truncate\n      Status = \"Success\"\n    )\n    \n  }, error = function(e) {\n    tibble(\n      FileName = fname, \n      RootNode = \"ERROR\", \n      NumChildren = NA, \n      ChildNodes = \"\", \n      RootAttributes = \"\",\n      Status = paste(\"Failed to read:\", e$message)\n    )\n  })\n})\n\n# Display preview\nprint(\"--- XML Data Dictionary Preview ---\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"--- XML Data Dictionary Preview ---\"\n```\n\n\n:::\n\n```{.r .cell-code}\nprint(head(report))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 2 × 6\n  FileName                 RootNode NumChildren ChildNodes RootAttributes Status\n  <chr>                    <chr>          <int> <chr>      <chr>          <chr> \n1 LaurentEtAl2025_BG_grid… PAMData…          31 Subdataset \"\"             Succe…\n2 LaurentEtAl2025_BG_H2_a… PAMData…           5 Subdataset \"\"             Succe…\n```\n\n\n:::\n:::\n\n\n\n\n\n\n\n\n## Save Results\n\nSave the dictionary to a CSV file for review.\n\n\n\n\n\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# Define output directory\noutput_dir <- file.path(\"Results\", \"Inspect_xml\")\ndir.create(output_dir, recursive = TRUE, showWarnings = FALSE)\n\n# Define filename\noutput_file <- file.path(output_dir, paste0(\"XML_Dictionary_\", Sys.Date(), \".csv\"))\n\n# Write CSV\nwrite.csv(report, output_file, row.names = FALSE)\n\nprint(paste(\"Data Dictionary saved to:\", output_file))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"Data Dictionary saved to: Results/Inspect_xml/XML_Dictionary_2025-12-09.csv\"\n```\n\n\n:::\n:::\n\n\n\n\n\n\n\n\n## Curation Insights\n\nUse the generated CSV to perform these checks:\n\n-   **Validation:** While this script checks for well-formedness (can it be parsed?), it does not validate against a specific schema (XSD). If a schema is available, consider using `xml2::xml_validate()`.\n\n-   **Root Node Consistency:** Check if all files share the same `RootNode`. Inconsistent root nodes might indicate different file types mixed together (e.g., metadata records mixed with configuration files).\n\n-   **Empty Files:** Check `NumChildren`. If 0, the XML file might be empty or contain only a root element with no data.\n\n## Using the Non-Interactive R Script\n\nFor users who want to run this analysis on a server (HPC), in a batch job, or from the command line, here is the pure R script version.\n\n### The `Inspect_xml_Script.R` Script\n\nDownload the **R Script:** [**`Inspect_xml_Script.R`**](Scripts/Inspect_xml_Script.R)\n\n### Example HPC Submission Script (`Inspect_xml_submit.sh`)\n\n``` bash\n#!/bin/bash\n#SBATCH --nodes=1\n#SBATCH --ntasks=1\n#SBATCH --time=00:10:00\n#SBATCH --job-name=xml_check\n\n# Load R module\nmodule load R\n\n# Define data directory\nDATA_DIR=\"/scratch/your_user/your_project/xml_data\"\n\n# Run Script\nRscript Scripts/Inspect_xml_Script.R $DATA_DIR\n```\n",
    "supporting": [
      "Inspect_xml_Notebook_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {
      "knitr": [
        "{\"type\":\"list\",\"attributes\":{},\"value\":[]}"
      ]
    },
    "preserve": null,
    "postProcess": false
  }
}